<nav class="border border-3 border-start-0 border-end-0 navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid mb-0 align-items-center text-center position-relative d-flex justify-content-center">
      <ul class="navbar-nav mb-0 mb-lg-0">
      <% Service.all.each do |service|%>
        <li class="nav-item mb-0 p-1">
          <a class="nav-link active fw-bold text-secondary" aria-current="page" href="/services/<%=service.id%>"><%=service.name%></a>
        </li>
        <%end%>
      </ul>
    </div>
  </div>
</nav>
