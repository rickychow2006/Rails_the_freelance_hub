<nav class="border border-3 border-start-0 border-end-0 navbar navbar-expand-lg navbar-light bg-light d-flex justify-content-center align-items-center">
  <div class="container-fluid mb-0 align-items-center text-center position-relative">
      <ul class="navbar-nav mb-0 mb-lg-0">
      <% Service.all.each do |service|%>
        <li class="nav-item padding-8px">
          <a class="nav-link active fw-bold" aria-current="page" href="/services/<%=service.id%>"><%=service.name%></a>
        </li>
        <%end%>
      </ul>
    </div>
  </div>
</nav>
